{
    "$schema" : "http://json-schema.org/draft-03/schema",
    "title" : "glTF",
    "type" : "object",
    "description" : "The glTF model.",
    "properties" : {
        "asset" : {
            "$ref" : "asset.schema.json"
        },
        "buffers" : {
            "description" : "The buffers used in this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "buffer.schema.json"
            }
        },
        "cameras" : {
            "description" : "The cameras available to this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "camera.schema.json"
            }
        },
        "images" : {
            "description" : "The images used in this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "image.schema.json"
            }
        },
        "materials" : {
            "description" : "The materials used in this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "material.schema.json"
            }
        },
        "meshes" : {
            "description" : "The meshes that make up this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "mesh.schema.json"
            }
        },
        "nodes" : {
            "description" : "The nodes that make up this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "node.schema.json"
            }
        },
        "shaders" : {
            "description" : "The shaders used in this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "shader.schema.json"
            }
        },
        "techniques" : {
            "description" : "The techniques used in this model.",
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "technique.schema.json"
            }
        },
        "version" : {
            "type" : "string",
            "description" : "The glTF version.",
            "required" : true
        },
        "extra" : {
            "type" : "any",
            "description" : "Optional application-specific data."
        }
    },
    "additionalProperties" : false
}
