{
    "$schema" : "http://json-schema.org/draft-03/schema",
    "title" : "animation channel target",
    "type" : "object",
    "description" : "The glTF property to target from an animation channel.",
    "properties" : {
        "id" : {
            "type" : "string",
            "description" : "The id (JSON property name) of the node to target.",
            "required" : true
        },
        "path" : {
            "type" : "string",
            "description" : "The node (specified by id) property name to modify.",
            "enum" : ["translation", "rotation", "scale"],
            "required" : true
        },
        "extensions" : {
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "extension.schema.json"
            }
        },
        "extras" : {
            "$ref" : "extra.schema.json"
        }
    },
    "additionalProperties" : false
}
