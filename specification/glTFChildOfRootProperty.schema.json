{
    "$schema" : "http://json-schema.org/draft-03/schema",
    "title" : "Child of a glTF root property",
    "type" : "object",
    "properties" : {
        "name" : {
            "type" : "string",
            "description" : "The user-defined name of this object.  This is not necessarily unique among objects in the parent property, e.g., accessors, buffers, etc."
        },
        "extensions" : {
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "extension.schema.json"
            }
        },
        "extras" : {
            "$ref" : "extras.schema.json"
        }
    }
}
