{
    "$schema" : "http://json-schema.org/draft-03/schema",
    "title" : "program",
    "type" : "object",
    "description" : "program",
    "properties": {
        "attributes" : {
            "type" : "array",
            "description" : "Names of GLSL vertex shader attributes.  For use with bindAttribLocation().",
            "items" : {
                "type" : "string"
            }
        },
        "fragmentShader" : {
            "type" : "string",
            "description" : "The id (property name) of the fragment shader referenced by this program.",
            "required" : true
        },
        "name" : {
            "type" : "string",
            "description" : "The user-defined name of this program.  This is not necessarily unique among programs."
        },
        "vertexShader" : {
            "type" : "string",
            "description" : "The id (property name) of the vertex shader referenced by this program.",
            "required" : true
        },
        "extensions" : {
            "properties" : {
            },
            "additionalProperties" : {
                "$ref" : "extension.schema.json"
            }        
        },
        "extras" : {
            "type" : "any",
            "description" : "Optional application-specific data."
        }
    },
    "additionalProperties" : false
}
