{
    "$schema" : "http://json-schema.org/draft-03/schema",
    "title" : "bufferView",
    "type" : "object",
    "description" : "Represents a view of a buffer for indexing and manipulation",
    "properties" : {
        "buffer" : {
            "type" : "string",
            "description" : "The id (JSON property name) of the buffer referenced by this view.",
            "required" : true
        },
        "byteOffset" : {
            "type" : "integer",
            "description" : "The offset into the buffer, in bytes.",
            "minimum" : 0,
            "default" : 0
        },
        "byteLength" : {
            "type" : "integer",
            "description" : "The length of the buffer view, in bytes.",
            "minimum" : 0,
            "required" : true
        },
        "target" : {
            "type" : "integer",
            "description" : "Target type for bindBuffer(), either 34962 (ARRAY_BUFFER) or 34963 (ELEMENT_ARRAY_BUFFER). When not provided, the buffer view contains animation or skin data.",
            "enum" : [34962, 34963]
        },
        "extras" : {
            "type" : "any",
            "description" : "Optional application-specific data."
        }
    },
    "additionalProperties" : false
}
