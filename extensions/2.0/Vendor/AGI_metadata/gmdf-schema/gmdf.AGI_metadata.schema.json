{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "AGI_metadata glTF extension",
    "type": "object",
    "description": "glTF Extension that defines metadata for applying external analysis or effects to a model.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "attachPoints": {
            "type": "array",
            "description": "An array of node names of attach points.",
            "items": {
                "type": "string"
            },
            "minItems": 1,
            "gltf_detailedDescription": "An array of glTF node names of attach points.  An attach point is a position on the model, within the node hierarchy, where external objects, effects, or analysis may be attached.  It is attached to the origin of the named node, and if orientation is needed, it is derived from the node as well.  This field is only valid in a gmdf external file.  For embedded metadata, use node.AGI_metdata.isAttachPoint instead."
        },
        "articulations": {
            "type": "array",
            "description": "An array of articulations.",
            "items": {
                "$ref": "gmdf.articulation.schema.json"
            },
            "minItems": 1,
            "gltf_detailedDescription": "An array of articulations.  An articulation indicates a named range of motion available to one or more nodes within the model."
        },
        "solarPanelGroups": {
            "type": "array",
            "description": "An array of solar panel groups.",
            "items": {
                "$ref": "gmdf.solarPanelGroup.schema.json"
            },
            "minItems": 1
        },
        "extensions": { },
        "extras": { }
    }
}
