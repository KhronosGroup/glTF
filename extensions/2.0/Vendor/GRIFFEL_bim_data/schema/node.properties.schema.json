{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Node Properties and Type",
    "type": "object",
    "description": "Maps properties to nodes by node index. Used only when data is saved in a separate file.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "node": {
            "allOf": [ { "$ref": "glTFid.schema.json" } ],
            "description": "Index of the node index in glTF file"
        },
        "properties": {
            "type": "array",
            "item": {
                "type": "number",
                "description": "Index of a property from the root level collection."
            },
            "description": "Collection of indices which point to corresponding instance properties of the node. (Instance properties are unique to the node.)",
            "minItems": 1
        },
        "type": {
            "type": "number",
            "description": "Index of a type in the root level collection. (Type is a set of properties which are common for many nodes.)"
        },
        "extensions": { },
        "extras": { }
    },
    "required": [ "node", "properties" ]
}