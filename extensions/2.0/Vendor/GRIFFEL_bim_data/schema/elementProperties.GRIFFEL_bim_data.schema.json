{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Element type and instance properties.",
    "type": "object",
    "description": "Maps properties to Nodes by name.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "node": {
            "type": "string",
            "description": "Name of the Node which has type and properties below."
        },
        "properties": {
            "type": "array",
            "description": "Collection of indices which point to corresponding instance properties of the node. (Instance properties are unique to the Node.)"
        },
        "type": {
            "type": "number",
            "description": "Index of corresponding type of the node. (Type is a set of properties which are common for many Nodes.)"
        },
        "extensions": { },
        "extras": { }
    },
    "required": [ "node", "properties" ]
}