{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mesh.primitive.KHR_mesh_primitive_visibility_hint.schema.json",
  "title": "KHR_mesh_primitive_visibility_hint glTF Mesh Primitive Extension",
  "description": "Annotates a mesh primitive with a visibility role for view contexts such as first-person or third-person. Applies to this primitive only (no inheritance). Advisory: the runtime resolves visibility from the active view context according to the role's semantics. Use when a view-context-specific region is baked into a mesh that cannot be separated by node.",
  "type": "object",
  "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
  "properties": {
    "role": {
      "type": "string",
      "description": "The view context in which this primitive is visible. Standard vocabulary values ('always', 'first_person', 'third_person') provide interoperability guarantees; custom, vendor-prefixed values are permitted for domain-specific view contexts.",
      "minLength": 1
    },
    "label": {
      "type": "string",
      "description": "Human-readable label for this visibility annotation. Used for UI display in authoring tools and inspectors.",
      "minLength": 1
    },
    "extensions": { },
    "extras": { }
  },
  "required": ["role"]
}
