{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "node.KHR_node_visibility_hint.schema.json",
  "title": "KHR_node_visibility_hint glTF Node Extension",
  "description": "Marks a node (and, by inheritance, its subtree) as visible only in a particular view context, such as first-person or third-person. Advisory: the runtime resolves the active view context and applies the resulting visibility through KHR_node_visibility semantics.",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The view context in which this node is visible. Standard vocabulary values ('both', 'first_person_only', 'third_person_only') provide interoperability guarantees; custom, vendor-prefixed values are permitted for domain-specific view contexts.",
      "minLength": 1
    },
    "label": {
      "type": "string",
      "description": "Human-readable label for this visibility annotation. Used for UI display in authoring tools and inspectors.",
      "minLength": 1
    }
  },
  "required": ["role"],
  "additionalProperties": false
}
