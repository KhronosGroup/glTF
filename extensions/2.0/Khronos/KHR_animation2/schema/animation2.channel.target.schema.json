{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "animation2.channel.target.schema.json",
    "title": "Animation Channel Target",
    "type": "object",
    "description": "The descriptor of the animated property.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "node": {
            "allOf": [ { "$ref": "glTFid.schema.json" } ],
            "description": "The index of the node to animate, but the value is ignored when using this extension. This is just for backward compatibility."
        },
        "path": {
            "type": "string",
            "description": "When using this extension, the path has to be a JSON pointer of the object to target."            
        },
        "extensions": { },
        "extras": { }
    },
    "required": [ "path" ]
}
