{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Animation Channel",
    "type": "object",
    "description": "Targets an animation's sampler at a object's property.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "sampler": {
            "allOf": [ { "$ref": "glTFid.schema.json" } ],
            "description": "The index of a sampler in this animation used to compute the value for the target.",
            "gltf_detailedDescription": "The index of a sampler in this animation used to compute the value for the target, e.g., a node's translation, rotation, or scale (TRS)."
        },
        "targetpointer": {
            "type": "string",
            "description": "The JSON pointer of the object to target.",
            "gltf_detailedDescription": "The number elements of the target **MUST** have the same number of output elements of the sampler."
        },
        "extensions": { },
        "extras": { }
    },
    "required": [ "sampler", "target" ]
}
