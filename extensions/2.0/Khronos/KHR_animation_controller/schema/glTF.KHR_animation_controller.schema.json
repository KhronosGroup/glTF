{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title" : "KHR_animation_controller glTF extension",
    "type": "object",
    "description": "Animation controller for glTF animations.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "controllers": {
            "type": "array",
            "description": "An array of animation controllers.",
            "items": {
                "$ref": "controller.schema.json"
            },
            "minItems": 1,
            "gltf_detailedDescription": "An array of animation controllers. A contoller provides the current timestamp used for the input field of an animation sampler."
        },
        "extensions": { },
        "extras": { }
    }
}
