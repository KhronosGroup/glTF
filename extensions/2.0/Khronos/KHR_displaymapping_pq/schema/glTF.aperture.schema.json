{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "Aperture settings",
    "type": "object",
    "description": "Defines settings for aperture.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "apertureValue": {
            "type": "number",
            "description": "Specifies the aperture value."
            "minItems": 1,
        },
        "apertureValue": {
            "type": "number",
            "description": "Specifies the aperture control."
            "minItems": 1,
        },
        "minLuminance": {
            "type": "number",
            "description": "Specifies the min luminance used in aperture calculations."
            "minItems": 1,
        },
        "maxLuminance": {
            "type": "number",
            "description": "Specifies the max luminance used in aperture calculations."
            "minItems": 1,
        }
    },
}