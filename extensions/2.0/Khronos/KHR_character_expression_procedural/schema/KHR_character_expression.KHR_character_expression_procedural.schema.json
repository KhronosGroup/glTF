{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "KHR_character_expression_procedural KHR_character_expression Extension",
    "type": "object",
    "description": "glTF extension that extends KHR_character_expression to specify procedural expression animations.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "conflictResolution": {
            "type": "string",
            "description": "Conflict resolution strategy used to determine how multiple sources (procedural and non-procedural) control a given expression.",
            "enum": ["none", "block", "blend"],
            "default": "none"
        },
        "extensions": { },
        "extras": { }
    }
}
