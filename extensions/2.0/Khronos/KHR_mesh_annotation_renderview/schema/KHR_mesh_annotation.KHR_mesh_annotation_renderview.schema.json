{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "KHR_mesh_annotation_renderview KHR_mesh_annotation Extension",
    "type": "object",
    "description": "glTF extension that provides structured metadata for character mesh primitives related to visibility and render-time behavior.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "renderVisibility": {
            "type": "string",
            "description": "Controls camera-based visibility.",
            "enum": ["always", "firstPersonOnly", "thirdPersonOnly", "never"],
            "default": "always"
        },
        "extensions": { },
        "extras": { }
    }
}
