{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "behaviorNode/branch/if",
    "type": "object",
    "description": "Behavior node brnach if definition.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "condition": {
            "allOf": [ { "$ref": "glTFid.schema.json" } ],
            "description": "The index of the condition behavior node."
        },
        "then": {
            "allOf": [ { "$ref": "glTFid.schema.json" } ],
            "description": "The index of the next behavior node to be executed in the true condition case."
        },        
        "else": {
            "allOf": [ { "$ref": "glTFid.schema.json" } ],
            "description": "The index of the next behavior node to be executed in the false condition case."
        },        
        "extensions": { },
        "extras": { }
    },
    "required": [
        "condition", "then"
    ]
}
