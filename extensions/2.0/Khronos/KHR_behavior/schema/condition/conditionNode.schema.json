{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "conditionNode",
    "type": "object",
    "description": "Condition node definition.",
    "allOf": [ { "$ref": "glTFChildOfRootProperty.schema.json" } ],
    "properties": {
        "operator": {
            "description": "Allowed operators.",
            "anyOf": [
                {
                    "const": "NOT"
                },
                {
                    "const": "OR"
                },
                {
                    "const": "AND"
                },
                {
                    "const": "EQUAL"
                },
                {
                    "const": "UNEQUAL"
                },
                {
                    "const": "LESS"
                },
                {
                    "const": "LARGER"
                },
                {
                    "const": "LESSEQUAL"
                },
                {
                    "const": "LARGEREQUAL"
                },
                {
                    "type": "string"
                }
            ]
        },
        "variableNodes": {
            "type": "array",
            "description": "The indices of variable nodes. The number of array elements **MUST** match the required arguments which is 1 for `NOT` and 2 for the other operators.",
            "items": {
                "$ref": "glTFid.schema.json"
            }
        },        
        "name": { },
        "extensions": { },
        "extras": { }
    },
    "required": [
        "operator",
        "variableNodes"
    ]
}
