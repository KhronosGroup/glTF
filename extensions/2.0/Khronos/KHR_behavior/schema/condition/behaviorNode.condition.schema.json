{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "behaviorNode/condition",
    "type": "object",
    "description": "Behavior node condition definition.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "operator": {
            "description": "Allowed operators.",
            "anyOf": [
                {
                    "const": "NOT"
                },
                {
                    "const": "OR"
                },
                {
                    "const": "AND"
                },
                {
                    "const": "EQUAL"
                },
                {
                    "const": "UNEQUAL"
                },
                {
                    "const": "LESS"
                },
                {
                    "const": "LARGER"
                },
                {
                    "const": "LESSEQUAL"
                },
                {
                    "const": "LARGEREQUAL"
                },
                {
                    "type": "string"
                }
            ]
        },
        "inputBehaviorNodes": {
            "type": "array",
            "description": "The indices of behavior nodes used as input arguments. The number of array elements **MUST** match the required arguments which is 1 for `NOT` and 2 for the other operators.",
            "items": {
                "$ref": "glTFid.schema.json"
            }
        },        
        "extensions": { },
        "extras": { }
    },
    "required": [
        "operator",
        "inputBehaviorNodes"
    ]
}
